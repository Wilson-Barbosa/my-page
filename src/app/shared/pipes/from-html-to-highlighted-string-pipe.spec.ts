import { FromHtmlToHighlightedStringPipe } from './from-html-to-highlighted-string-pipe';

describe('FromHtmlToHighlightedStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FromHtmlToHighlightedStringPipe();
    expect(pipe).toBeTruthy();
  });
});
